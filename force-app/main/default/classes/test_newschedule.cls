@isTest
private class test_newschedule 
{
    public static testmethod void test1()
    {
        Test.startTest();
      newschedule newschdl = new newschedule();
        newschdl.execute(null);
        Test.stopTest();
        
    }
}